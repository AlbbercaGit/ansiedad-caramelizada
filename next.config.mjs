/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/ansiedad-caramelizada',
    images: {
      unoptimized: true,
    },
  };
  
  export default nextConfig;